
This container can be used to develop python 3 applications.

For example:

docker run -it --rm \
  --name python3-devel \
  -u `id -u`:`id -g` \
  -v `pwd`:/opt/develop \
  -w /opt/develop \
  jaraf/python3-devel:3.7.3-1 bash
